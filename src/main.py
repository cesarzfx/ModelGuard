# ... imports ...


# (← keep exactly two blank lines above)
def _model_key_from_url(url: str) -> str | None:
    ...


# (← two blank lines)
def _default_for(url: str, kind: str) -> float | None:
    ...


# (← two blank lines)
def _unit(url: str, kind: str) -> float:
    # keep your default short-circuit here
    ...
